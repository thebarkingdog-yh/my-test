name: Run Python tests

on: [push]

jobs:
  build:
    name: Ruff Check
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - uses: jpetrucciani/ruff-check@main
        with:
          path: "."
