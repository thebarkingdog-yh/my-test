name: Run Ruff Check

on:
  push:
    branches:
      - main

jobs:
  ruff:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Run Ruff Check
        uses: jpetrucciani/ruff-check@main
        with:
          path: "."